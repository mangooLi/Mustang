import "reflect-metadata";
import "mocha";
import {CacheManager} from "../src/repository/CacheManager";

describe('cache manager', () => {
    let cache: CacheManager;

    before(async () => {
        
    });


});